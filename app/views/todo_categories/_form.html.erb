<%= form_with(model: @todo_category, local: true) do |f| %>
  <% if todo_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_category.errors.count, "error") %> prohibited this todo_category from being saved:</h2>

      <ul>
        <% todo_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %>
    <%= f.collection_select(:category_id ,Category.all, :id , :title) %>
  </div>

  <div class="field">
    <%= f.label :todo_id %>
    <%= f.collection_select(:todo_id ,Todo.all, :id , :title) %>
    
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
